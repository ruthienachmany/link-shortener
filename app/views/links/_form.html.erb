<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <hr>

  <fieldset>


          <div class="actions">

        <class="btn btn-primary btn-lg btn-block"> </class>
    <div class="login-form">
        
    
   <%= f.text_field :long_link, :class => "form-control login-field", :required => true %>
</div><br><br>
    <%= f.submit 'Shall I shorten that for you?', :class => 'btn btn-warning btn-lg btn-block'%>
    
    
</div>

<% end %>
  </div>
