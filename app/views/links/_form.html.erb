<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <hr>

  <fieldset>


    <p>
      <%= f.label :long_link %>
      <%= f.text_field :long_link, :class => "input-large span12", :required => true %>
    </p>
    
    <div class="clearfix"></div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
